export function DashboardPage() {
  return (
    <section className="panel">
      <p className="eyebrow">Project Structure Ready</p>
      <h2>MVP screens and game systems land next.</h2>
      <p>
        This placeholder confirms the React + TypeScript app is wired and ready
        for the product build-out.
      </p>
    </section>
  );
}
